import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

interface AdShowCaseProps {
}

const AdShowCase: React.FC<AdShowCaseProps> = async () => {

    return <Card>
        <CardHeader><CardTitle>No Entry available</CardTitle></CardHeader>
    </Card>;
};

export default AdShowCase;